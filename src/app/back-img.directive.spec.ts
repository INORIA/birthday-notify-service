import { BackImgDirective } from './back-img.directive';

describe('BackImgDirective', () => {
  it('should create an instance', () => {
    const directive = new BackImgDirective();
    expect(directive).toBeTruthy();
  });
});
